\begin{table}[h]
\centering
\caption{Algorithms for the update-aware sorting model}
\label{tab:incremental-sorting-algorithms}
\begin{tabular}{l c c}
\toprule
Algorithm & Time & Space \\
\midrule
Binary-Insertion-Sort & $O(kn)$ & $O(1)$ \\
Extract-Sort-Merge* & $O(k \log k + n)$ & $O(n)$ \\
\textbf{DeltaSort}* & $O(k \log n + n)$ & $O(k)$ \\
\bottomrule
\end{tabular}

\vspace{0.3em}
{$n$ = array size, $k$ = number of updates}

\small *These algorithms rely on a sort primitive assumed to run in $O(k \log k)$ time and $\le O(k)$ space.
\end{table}