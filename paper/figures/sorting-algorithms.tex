\begin{table}[t]
\centering
\caption{Algorithm complexity comparison}
\label{tab:complexity}
\begin{tabular}{l c c c c}
\toprule
Algorithm & Comparisons (C) & Movement (M) & Space (S) \\
\midrule
FullSort$^{*}$ $(\texttt{sort\_by})$ & $O(n \log n)$ & - & - \\
BIS & $O(k \log n)$ & $\Theta(nk)$ & $O(1)$ \\
ESM & $O(k \log k + n)$ & $\Theta(n)$ & $O(k)$ \\
\textbf{DeltaSort} & $O(k \log n)$ & $\Theta(n\sqrt{k})^{\dagger}$ & $O(k)$ \\
\bottomrule
\end{tabular}

\vspace{0.3em}
{\small $^{*}$Not update-aware; hence complexity is a function of only $n$. The exact movement and space complexity are implementation-dependent and not required for the present discussion.}


{\small $^{\dagger}$Expected. Worst-case movement is $\Theta(nk)$}
\end{table}